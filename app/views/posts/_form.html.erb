<%= form_with model: @post do |form| %>
    <%= form.text_field :title, class: 'form-control', placeholder: 'Título' %>
    <%= form.rich_text_area :body, class: 'form-control', placeholder: 'Contenido' %>

    <%= form.submit 'Guardar', class: 'btn btn-primary' %>
<% end %>